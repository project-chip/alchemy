{{#ifSet action.Variable}}
{{#ifFieldIsArray action.Field}}
asserts.assert_greater_equal(len({{action.Variable}}), {{minConstraint action}}, "{{attributeName step action }} must have at least {{minConstraint action}} entries!")
{{else ifFieldHasLength action.Field}}
asserts.assert_greater_equal(len({{action.Variable}}), {{minConstraint action}}, "{{attributeName step action }} must be at least {{minConstraint action}} long!")
{{else}}
asserts.assert_greater_equal({{action.Variable}}, {{minConstraint action}})
{{/ifFieldIsArray}}
{{/ifSet}}