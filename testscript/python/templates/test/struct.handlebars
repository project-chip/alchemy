async def test_check{{step.name}}(self, 
                             endpoint: int = None, 
                             cluster: Clusters.{{clusterName test}} = None, 
                             struct: {{structFullName test step step.Entity}} = None):
{{#each step.Actions}}
    {{> test/struct_field test=test step=step action=this}}
{{/each}}
