{{#if step.Response.Error}}
await self.read_single_attribute_expect_error(endpoint=endpoint, cluster=cluster, attribute=cluster.Attributes.{{step.Attribute}})
{{else}}
{{#if step.Response.SaveAs}}{{step.Response.SaveAs}} = {{else if step.Response.Constraints}}val = {{else if step.Response.Value}}val = {{/if}}await self.read_single_attribute_check_success(endpoint=endpoint, cluster=cluster, attribute=cluster.Attributes.{{step.Attribute}}{{#if step.FabricFiltered}}fabricFiltered=True{{/if}})
{{/if}}
{{> constraints test=test step=step}}