{{#if step.Response.Constraints.HasMasksSet}}
asserts.assert_true(all([({{> test/commands/response_value step=step}} & mask) != 0 for mask in {{pythonValue step.Response.Constraints.HasMasksSet}}])
{{/if}}