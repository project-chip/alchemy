{{descriptionComment cluster.Description ~}}
cluster {{asUpperCamelCaseIgnoreAcronyms cluster.Name}} = {{number cluster.ID}} {
  revision {{currentRevision cluster.Revisions}};
{{#enums cluster.ReferencedEnums}}

  {{> enum cluster=cluster.Cluster shared=@shared enum=this provisional=@provisional}}
{{/enums}}
{{#bitmaps cluster }}

  {{> bitmap cluster=cluster.Cluster bitmap=this provisional=@provisional}}
{{/bitmaps}}
{{#structs cluster.ReferencedStructs}}

  {{> struct cluster=cluster.Cluster struct=this provisional=@provisional}}
{{/structs}}
{{#events cluster.Cluster}}

  {{> event cluster=cluster.Cluster event=this provisional=@provisional }}
{{/events}}

{{#attributes cluster.Cluster}}
  {{> attribute cluster=cluster  attribute=this provisional=@provisional}}
{{/attributes}}
{{#commands cluster.Commands}}
{{#ifRequest this ~}}
{{#ifHasValidFields this.Fields}}

  {{> request cluster=cluster.Cluster command=this provisional=@provisional}}
{{/ifHasValidFields}}
{{else}}

  {{> response cluster=cluster.Cluster command=this provisional=@provisional}}
{{/ifRequest}}
{{/commands}}
{{#if cluster.Commands}}

{{#requests cluster.Commands}}
  {{> command cluster=cluster command=this provisional=@provisional}}
{{/requests}}
{{/if}}
}
