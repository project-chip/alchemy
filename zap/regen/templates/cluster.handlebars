{{descriptionComment cluster.Description ~}}
cluster {{asUpperCamelCase cluster.Name}} = {{number cluster.ID}} {
  revision {{currentRevision cluster.Revisions}};
{{#enums cluster.Enums}}

  {{> enum cluter=cluster shared=@shared enum=this}}
{{/enums}}
{{#bitmaps cluster}}

  {{> bitmap cluter=cluster bitmap=this}}
{{/bitmaps}}
{{#structs cluster.Structs}}

  {{> struct cluter=cluster struct=this}}
{{/structs}}
{{#events cluster}}

  {{> event cluter=cluster event=this}}
{{/events}}

{{#attributes cluster}}
  {{> attribute cluster=cluster provisional=@provisional attribute=this}}
{{/attributes}}
{{#commands cluster.Commands}}
{{#ifRequest this ~}}
{{#if this.Fields}}

  {{> request cluster=cluster command=this}}
{{/if}}
{{else}}

  {{> response cluster=cluster command=this}}
{{/ifRequest}}
{{/commands}}
{{#if cluster.Commands}}

{{#requests cluster.Commands}}
  {{> command cluster=cluster command=this}}
{{/requests}}
{{/if}}
}
